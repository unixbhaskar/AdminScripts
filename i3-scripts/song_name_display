#!/bin/bash

mpv_pid=$(pgrep mpv)

playing=$( ps -ef | grep mpv | sort -r | gawk '{ print $10 }')

songs=$(basename $playing)
songs2=${songs%.*}

if [[ $mpv_pid != "" ]];then

    #echo "$songs2"
    echo -e "<span color='black' bgcolor='#FF1493'>$songs2</span>\n"
fi
